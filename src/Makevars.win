# PKG_CPPFLAGS
# PKG_CFLAGS
# PKG_LIBS

# We build a dummy chli.dll to satisfy fame externals
CPPFLAGS += -Dfame

fame.dll: chli.dll

chli.dll: chli.c
	gcc  -shared -s  -o chli.dll chli.c -DDUMMY_CALLS
